spring:
  datasource:
    url: jdbc:tc:mysql:8:///test_book_store_db
    username: test
    password: test
  jpa:
    show-sql: true
bookstore:
  openapi:
    dev-url: http://localhost:8080
jwt:
  expiration: ${JWT_EXPIRATION:600000}
  secret: ${JWT_SECRET:somesecretkey-10-=1=falkaflk91900913fkjaf19080}
